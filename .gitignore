# y-sync 仓库默认忽略清单
# 注意：客户端内置的同步忽略默认清单（.y-sync/ .git/ *.tmp 等）在
# internal/client/ignore.go 的 defaultPatterns 中，与本文件用途不同。

# 构建产物
/bin/
/dist/

# 服务端本地数据目录（测试时产生的 SQLite + blob）
/y-sync-data/
**/y-sync-data/

# 客户端本地状态目录（若在仓库内做测试）
**/.y-sync/

# 测试与日志
*.log
*.out
coverage.*

# 编辑器与系统文件
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# 本地环境配置（勿提交真实地址/凭据）
.env
*.local.toml
deploy/deploy.env

# Rust 构建产物
/target/
