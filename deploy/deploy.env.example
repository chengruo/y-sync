# 本地手动部署用的环境文件（复制为 deploy.env，已被 .gitignore 忽略）
export DEPLOY_HOST="124.221.205.250"
export DEPLOY_USER="ubuntu"
export DEPLOY_KEY_FILE="$HOME/.ssh/y-sync-deploy"   # 私钥路径（或用 DEPLOY_KEY 传内容）
export DEPLOY_DOMAIN="sync.example.com"             # 仅 bootstrap 用
